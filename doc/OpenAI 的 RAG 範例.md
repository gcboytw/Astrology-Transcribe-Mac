---
created: 2026-03-27T14:11
---

# OpenAI 的 RAG 範例 (AI 專業校正版)

### [00:00:00]

相信你一定用過各式各樣的智慧客服系統。就是使用者問一個問題，模型就可以從好如煙海的文件裡面找到相關的內容並且回答。在大部分情況下，我們都是用RAG來實現這種系統的。傳統的RAG會涉及到向量化相關的事情。但最近OpenAI分享了一個類似的問答系統開發案例。它用的也是RAG，但是整個過程沒有涉及任何的向量化。有沒有好奇它是如何做到的呢？那今天我們就來一起看看吧。考慮到很多同學可能不太清楚RAG到底是一種什麼東西我先簡單介紹下。熟悉的同學可以直接跳過這段。我們舉一個具體點的例子。比如說你想做一個智慧客服。使用者可以透過這個客服。

### [00:00:44]

詢問你們公司某個產品相關的資訊。怎麼實現這樣的一個產品呢？首先如果想要回答使用者問題的話，你就必須要有一個大模型。當然只有一個大模型肯定也是沒有用的。因為模型不知道你們公司的產品資訊如果你直接讓他回答使用者問題的話他只能說不知道不過你可能在想那我把我的產品手冊也給大模型不就好了這當然可以。不過這種方法有很多問題。比如產品手冊可能會非常長。模型讀了後面忘了前面。而且讀這麼長的內容成分也很高。

### [00:01:17]

更不用說耗時也是一個很大的問題。所以就有一種叫做reg的技術出現了。它就是用來解決這個問題的。它可以從文件裡面抽取與使用者問題相關的內容。可能1000頁左右的商品手冊裡面。只有三段話是與使用者問題相關的他就把這三段話給找出來別的無關的都不要然後就把這部分相關的內容與使用者的問題一起發給模型這樣模型有了相關資訊之後，就可以回答使用者問題了。這種先抽取相關內容，再根據這些內容回答使用者問題的方式，就叫做“reg”了。

### [00:01:49]

當然，reg有很多實現方式。不同的實現方式會用不同的方法。從文件中抽取與使用者問題相關的內容。再回頭看看我們之前畫的匹配過程。傳統的RAG是使用向量化這一技術來完成內容匹配的。向量化是一種把文字轉換為數字列表的過程。如果不知道的話你也不用管。我們影片其實不涉及。因為OpenAI所提的這個RAG方案根本就不需要向量化。好，RAG我們就先聊到這裡。下面我就給大家介紹一下OpenAI這個方案的整體鏈路。OpenAI在這裡做的是一個法律知識問答系統。它在回答使用者問題的時候，一共要做以下幾個事情。分別是檔案載入。

### [00:02:31]

內容切割內容挑選生成答案以及答案驗證這幾個環節其中內容切割和內容挑選會重複多次大家看這個流程可能會有點懵啊。這都是些什麼東西。不要擔心。我來一步步地給大家講解。講到哪一步。我就會把哪一步對應的框給標黃。比如這樣。讓大家跟著流程走。就會簡單很多。首先是檔案載入部分。

### [00:02:57]

我們的系統首先會去讀一個1000頁左右的法律檔案這個檔案是PDF格式的它會將這個PDF檔案中的文字內容提取出來放到一個字串中供後續使用拿到了檔案內容之後呢原來的PDF就沒有什麼用了我們把它從圖中引掉之後就來到了內容切割。和內容挑選這個環節了。我們之前說過。這兩個環節會重複多次。至於重複幾次。

### [00:03:24]

那是程式碼裡面的一個變數來決定的設定幾次都行我們這裡就以三次為例給大家看一下首先看看第一次的切割與挑選我們需要把這個法律內容切割為三個部分。然後把這三個部分打包。一起發給模型。讓模型看看哪一部分。與使用者的問題相關比如模型最後可能會選擇第一部分和第三部分到這裡第一輪的切割和挑選環節就結束了我們來到第二輪的內容切割。

### [00:03:51]

和挑選環節。之前不是挑選出了兩塊相關的內容嗎？我們分別對這兩個相關的內容做切割。每個內容切割三份。一共能拿到六份。然後我們把這六份再次發給模型。讓它挑選與使用者問題相關的部分。比如模型可能會選擇其中的這兩部分。到這裡。第二次的切割和挑選就結束了我們來到第三輪第三輪還是一樣我們拿出之前挑選好的內容再分別做切割分別切成三份最後一共能拿到六份模型從這六份裡面挑選出與使用者問題相關的內容比如這些內容可能與使用者的問題相關到這裡第三輪就結束了。

### [00:04:34]

整個內容切割和內容挑選環節也就隨之而結束了。沒錯。我們重複了三次切割和挑選操作。就是為了選出這幾個與使用者問題相關的部分。那你可能會問重複這麼多次幹什麼呀我們不如一次到位直接把文件切成幾百份把這幾百份一起發給模型讓模型從這幾百份裡面挑選三份。與使用者問題最相關的就可以了。這樣就不用重複這麼多次了。你這個方案當然也是可行的。不過效果大機率是不如重複操作的好。因為直接把幾百份發給模型的話，切的就太細碎了。

### [00:05:08]

模型很有可能會搞亂。不如讓模型這樣一點一點挑選的效果好。好。內容切割和內容挑選環節我們就說到這裡。然後我們來到生成答案環節。在這個環節中，我們首先把之前挑選出來的。這三個與使用者問題相關的內容塊取出來跟使用者問題放在一起打個包全部發給大模型這樣大模型就可以根據之前挑選的內容來給出一個初步的回答了。為什麼說是初步呢？

### [00:05:36]

因為這裡還沒有結束。我們還需要對答案做驗證。驗證沒有問題了。我們才能發給使用者所以我們下面就來到答案驗證環節在這個環節中我們首先取出模型的初步回答把它和使用者問題挑選的內容塊都放在一起打包傳送給另外一個模型做驗證讓它看看這個答案有沒有胡說八道到底是不是根據引用的內容來回答的如果驗證透過的話系統才會將這個初步回答標記為最終答案並且返回給使用者。到這裡，所有的流程就算都結束了。

### [00:06:09]

除了整個流程之外，還有一點你需要關心：那就是模型的選擇。我們重點看看其中的內容挑選。生成答案和答案驗證環節。這三個環節所使用的模型是不一樣的。內容挑選環節所使用的模型是GPT4.1mini。這主要是基於GPT4.1mini的上下文視窗大小和成本兩點來考慮的。首先，GPT-4.1mini的上下文視窗非常大。有100萬。在內容挑選環節中，我們要把大量的文字發給模型讓他去挑。所以模型的上下文視窗越大越好。100萬就是一個非常優秀的指標。其次我們要控制成本。

### [00:06:47]

因為模型輸入太多了。所以這個模型的價格也必須要足夠低廉。GPT-4.1mini的價格也比較低。所以這個環節選它比較合適。生成答案環節所使用的模型是GPT4.1。因為這個環節我們需要保證答案的準確性。如果我們依然沿用GPT4.1mini的話，模型胡說八道的機率會很大。所以我們需要選用一個更靠譜的模型。同時考慮到在這一環節中，我們對成本的控制並沒有那麼強的要求。畢竟模型的輸入沒有那麼多了。我們都已經篩選完了。所以GPD4.1就是一個不錯的選擇。答案驗證環節呢？

### [00:07:23]

我們用的是OSMini。這個模型的推理能力很強。很適合對答案的準確性做出判斷。當然，因為這是 OpenAI 出的範例。所以它使用的都是自家的模型。大家如果不用 OpenAI 的話，也可以按照類似的思路來使用其他廠商的模型。這個方法本身都是通用的。下面我們來看一下它的程式碼實現。它這個程式碼比較長。在詳細解讀之前，我們先總體過一遍。第一部分是載入PDF文件。第二部分是做內容切割。

### [00:07:55]

我們之前動畫演示的時候會把文件切割為三份。三這個數字其實有點小。但是好演示，所以我們選擇了三分相，實際上它這裡會把文件切割為20份。20這個切割分數應該算是比較適中的。第三部分是做內容挑選。就是這裡的 rootchunk 引數然後第四部分呢它是寫了另外一個函式叫做 navigateToParagraphs這個函式呢是專門負責重複呼叫之前的內容切割。和內容挑選邏輯的。

### [00:08:27]

這裡有一個引數叫做maxdepth。決定了重複次數。在後面呢。就是生成答案的環節了對應的是這裡面的generateanswer函式答案生成完畢之後我們不是要對答案做驗證嗎所以我們就來到了答案印證環節這裡核心函式是verifyanswer所以總體來看一下它這個程式碼塊跟我們前面所看的流程是完全匹配的瞭解了主要框架之後我們再仔細過一遍其中的每個環節。首先是載入文件部分。

### [00:09:01]

我先把網頁放大一下。讓大家看起來更方便一些。這裡面的核心函式是loaddocument。它有一個引數是叫做URL。這個URL是指向PDF檔案的。在函式的內部，它首先使用request庫。從這個URL裡面把檔案下載了下來。然後再使用pdfreader這個庫載入了pdf文件載入完了之後它會去讀取這個pdf文件中前920頁的內容並且放到fulltext變數中為什麼只讀920頁呢？OpenAI給出的解釋是：這個系統只會解答前920頁的內容。

### [00:09:39]

後面的部分不關心。系統也不負責解答。這個函式最後返回的也是這個fulltext的變數在返回之前，這個函式還會列印一些文件相關的資訊，比如說是字數。token數量等等方面需要注意標點符號的使用。寫完函式之後，它就寫了一段程式碼。去下載了這個函式。在下載過程中，它還給出了這個PDF文件的例子。我們可以開啟看看。心中有個概念。

### [00:10:04]

這個PDF文件就是這個樣子的。可以看出確實還是挺長的。有一個1000多頁的樣子，我們在網頁的下面，是可以看到，雕的日誌的，可以看到，這個PDF文件一共是包含1194頁。59萬個單詞。93萬個token。確實還是挺長的。這裡有一個數字要重點關注一下那就是token的數量93萬後面你就會看到我們分析文件內容

（注：原文中沒有出現需要修正的錯別字或同音異義詞，且句子結構簡單無需新增標點符號。）所使用的模型：是GBD4.1 mini

### [00:10:39]

這個模型的上下文視窗大小是100萬。93萬小於100萬所以我們用這個方案來分析PDF文件的內容還能接受。如果我們要分析的字數再多一些，多到token數量是100多萬甚至幾百萬的話，我們就不能原模原樣的去套這個方案了。至少要把這個方案改一改。比如再做一些額外的切割之類的記不記得之前我們說過它這裡面只會讀920的內容雖然OpenAI給出的解釋是後面的部分不需要但我估計真實的原因是如果讀取全部的PDF內容的話，總token數量很有可能就會超過100萬。這樣的話，模型讀了後面，就會忘了前面。

### [00:11:18]

效果也就會大打折扣了。然後我們再來看看內容切割部分。這句話沒有包含需要校正的錯別字或者同音異義詞，且與提供的占星學專有名詞庫無關。因此無需進行任何修改。

這句原文：這裡面的splitinto20chunks

由於該句子並非中文且未提供有意義的內容，故無法新增標點符號或進行其他形式的語言處理。函式顧名思義，這裡沒有需要修正的占星學相關錯別字或同音異義詞。但根據指示新增適當標點：

函式，顧名思義，……（注：由於原句缺乏具體內容以供完整標點，僅示範逗號使用）就是用來把文字切割為20塊的。它接受的引數有兩個。一個是text也就是要切割的文字內容另外一個是min_tokens也就是切割之後每一個部分所包含的最小tokens數它這裡面會把切割好的每一部分叫做一個chunk然後最後返回的是這個chunk列表這個chunk列表裡面的每一個元素都是一個chunk每個 chunk 是包含了兩個屬性。

### [00:11:57]

第一個 是 id。id 其實也就是 chunk 的編號了。從 0 開始依次往上遞增。第一個是 0。第二個是1第三個是2以此類推後面的這個text就是對應chunk的文字內容了執行之後，我們可以得到這樣一個結果：它打印出了每一個chunk的編號；以及對應的token數量；下面再來到內容挑選部分：這個 rootChunk 引數就負責把使用者的問題外加我們所切割的 chunks 列表傳給模型讓模型從這些 chunk 裡面挑選與使用者問題所相關的那些

### [00:12:39]

不過大家可以看到這個函式不僅有question和chunks兩個引數還有另外兩個分別是叫做depth和scratchpad我們一個一個來解釋。這個depth是代表深度。因為這個函式會被反覆的呼叫。每次呼叫的時候，所對應的depth值都會不一樣。第一次呼叫的時候，depth值為0。第二次呼叫的時候，depth值為1。以此類推。後面的Scratchpad你可以理解為就是用來儲存模型的思考內容的。

### [00:13:10]

這個系統是會要求模型。在挑選內容之前必須要思考。思考的內容就會放到Scratchpad裡面。並且還會帶到下輪的內容切割。在內容選擇環節中這樣選擇的準確性通常會更高一些RootChunks這個函式內部使用了一些FunctionCalling和TextFormat這類的技術程式碼量還挺多的。不過具體我們就不細看了。感興趣的可以自己再琢磨下。在下面的navigatetoparagraph。函式乾的就是重複切割。加入挑選的事情了換句話說，它就是不停地去呼叫我們前面所看到的，splitinto20chunks函式和 rootchunks 函式

### [00:13:55]

這兩個函式分別是做內容切割和內容挑選的navigatetoparagraph 函式一共是包括三個功能分別是DocumentText文件的內容Question使用者問題MaxDepth這個引數是決定了它要重複切割多少次這裡面的程式碼寫得比較複雜我們也就不細看了其實本質上乾的就是這個事情寫完之後，它呼叫了一下這個函式。

### [00:14:22]

並且把輸出結果列印了出來。我們在這裡，也可以驗證我們的分析。它首先是將文件做了切片一共是分成了20片然後呢他選擇了其中的這幾個部分然後他還把scratchpad也就是模型的思考過程給列印了出來到這句話為止第一次的內容切割加內容挑選就結束了後面的這個 split是屬於第二次的過程了。

### [00:14:50]

同樣，他首先是做了內容的切割。因為他會對第一次挑選出來的每一個chunk繼續做切割。所以它這裡顯示切割了多次。切割完了之後，同樣的，它還是會從切割之後的結果裡面。選擇出與使用者問題匹配的答案然後再打印出模型的思考過程因為這個Scratchpad是一個追加機制所以它會把第一層的也就是上一次的思考過程先打印出來。列印完了之後，再列印本層的思考過程：大概就是這個樣子了。

### [00:15:25]

這個是第二次。然後後面就是第三次。我們就不細看了。其實整個過程都一樣了。全部都切割並且挑選完了之後。他就選擇了這些chunk。可以看出這個chunk數量還是有點多，所以他在這裡面就做了一個比較粗暴的篩選；他直接選擇了前三個chunk，作為最終與使用者問題匹配的內容了。說實話，他這個選擇確實是有些暴力。其實也有一些別的比較好的解決方案。比如說我們把選擇出來的所有的與使用者問題相關的這些chunk，再次發給模型讓它最後做一次細篩。

### [00:16:07]

篩選出其中三個與使用者問題最為相關的chunk下面它就打印出了這三個挑選的chunk的內容這個是第一個這後面是第二個這一塊就是第三個了到這裡內容切割加內容挑選的環節就結束了。在後面我們就來到了生成答案環節。這段程式碼的核心函式是generateAnswer。它一共包含了三個入參。分別是question代表使用者問題。段落與使用者問題最為相關的三段引用內容還有一個是scratchpad也就是模型的思考過程在傳入這三個引數之後呢它就會生成一個最終答案了這段程式碼寫完之後呢，它也會把輸出的答案展示出來了。

### [00:17:01]

就是這個樣子的。在之後呢，我們就要來到答案驗證環節了。就是這裡面的VerifyAnswer函式在這個函式裡面我們給它傳入的入參呢分別是Question使用者問題Answer前面所生成的答案SidedParagraphs就是我們所引用的段落內容也就是那三段的內容我們希望模型去驗證這個答案是否根據引用的內容？來回答了使用者的問題有沒有發生胡說八道的情況？

### [00:17:34]

程式碼我們就不再細看了吧。我們直接來看看它的驗證結果。從最終輸出的結果中，我們就可以看出，模型判定這個答案透過，而且還給了一個confidence的值，代表模型對答案透過的信心程度。這個結論的信心有多少？這個值為high。表示模型對這個答案的認可程度很高。答案與匹配的內容的關聯性：應該是很強的。在這種情況下，這個答案基本上就沒有什麼問題了。驗證環節到這裡也就告一段落了。

### [00:18:05]

最後它還給出了執行這個方案一次的。所需的價格。是0.36美元。其實還是有點貴的。當然你可以不呼叫OpenAI的模型。你使用這個方案。搭幾個開源的本地模型。應該也是可以的。這樣的話，就是前期投入成本比較高。後面就只剩下維護成本了。最後OpenAI給出了這個方案。與傳統RAC方案相比的優勢和劣勢優勢是不需要提前向量化文件修改之後，可以立即生效。

### [00:18:37]

很方便。整個過程是模擬了人的閱讀方式。理論上來說，準確率會更高一些。還有一點：那就是模型可查詢的內容更廣。傳統的RAG模式通常只會侷限在文件中的某一小部分。準確率相對來說就大打折扣。當然這個方案也是存在一些問題的。比如說每次查詢的價格都很高。查詢時間更長。文件太大了呢也不行。那大致就是這樣子的了。OpenAI的這個RAG方案細分下來應該是屬於AgenticRAG。鑑於影片長度的限制，我就不給大家解釋AgenticRAG的含義了。

### [00:19:16]

有興趣的可以自己去查一下今天的影片就到這裡結束了我們下次再見拜拜
