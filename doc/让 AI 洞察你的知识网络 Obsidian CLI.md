---
created: 2026-03-23T10:05
---

# 让 AI 洞察你的知识网络 Obsidian CLI (AI 專業校正版)

### [00:00:00]
在這裡，思維變成了一棵樹。Obsedding已正式發布CLI，AI終於能夠直接看懂你的整個知識庫了。嘿你好，我是Blink，AI加知識管理的實踐者。如果你對Obsedding還不清楚，可以先看這條。今天就講清楚ObseddingCLI的基礎概念到真正用起來。如果你對CLI這個詞不熟，可以完整觀看。如果你知道CLI，想看看具體怎麼用，可以挑到第二章入門。首先，CLI到底是什麼？很多非技術背景的朋友第一反應是這是Obsidian出的一個AI助理或者agent嗎？其實完全不是。CLI的全稱是CommandLineInterface。也就是命令列介面。現在螢幕左邊就是我們平時用的ObsidianApp。它有按鈕有選單可以點選。

### [00:00:48]
屬於GUI，也就是圖形使用者介面。而右邊這個就是Obsidian CLI。也就是Obsidian的命令列操作介面。一個是由滑鼠點選。一個是鍵盤輸入指令。它們的操作物件都是 Obsidian 的筆記。我們來舉個最簡單的例子。比如你要新增一條筆記。在 Obsidian App 的圖形介面中，就是點選新建筆記這個按鈕。點選之後，新的筆記就已經建立了。然後你可以在介面中進行編寫。而在命令列介面中，我們要建立一條筆記。

### [00:01:20]
就要用到相關的命令。就是 create。create 一條筆記。然後這裡可以輸入它的名稱。對。然後名稱如果等於「NewNote」好。然後這時候就已經建立成功了。這裡就會出現一條叫「NewNote」的筆記。就建立成功了。對比來看，你可能覺得CLI的介面黑乎乎的，全是字，很抽象也很復古，你說對了。

### [00:01:46]
在計算機剛誕生的年代，滑鼠還沒有發明出來。也沒有高階的圖形顯示技術。所有的操作都是像這樣用鍵盤輸入指令來完成的。以前的所有程式都是命令列操作介面。直到1970年施勒研發出了圖形介面後來蘋果又把這個技術用在了個人電腦上GUI才開始普及你可能會問既然 APP 用得好好的，我為什麼要去敲命令呢？為了復古情懷嗎？當然不是。Obsidian CLI 不是給人類用的。

### [00:02:16]
而是給AI用的人類用起來很抽象而AI可以瞬間學會這些命令也可以迅速將多條命令組合起來實現大量而複雜的操作但讓AI用起來的前提是，你得先教AI怎麼用。接下來看實際操作，第一步啟用ObsidianCLI，首先開啟Obsidian的設定，確保你的版本是1.12.4或者更高的版本。然後找到下面這個命令列介面。把它開啟。然後這裡提示要註冊路徑點註冊。好。

### [00:02:48]
然後我們來驗證一下。開啟終端。輸入Obsidian。如果能看到這個介面。就說明已經啟用成功。第二步交會AI使用以CloudCode為例要交會AI技能當然首選是新增Skill而恰好Obsidian的CEOKapano親自編寫了一個Obsidian CLI的Skill我們可以在GitHub上搜索這個Obsidian Skill其中就包含了Obsidian CLI 的這個技能然後推薦這裡建議的Marketplace 的這個命令來進行安裝然後個人建議修改一下

### [00:03:19]
Cloud.md檔案在裡面備註使用Obsidian CLI的規範例如在進行Obsidian相關操作時優先使用Obsidian CLI的技能去呼叫命令所有的命令都加上silent除非我刻意說明要在前端開啟，這樣不會每完成一次操作都給你彈出一個視窗。還有就是標籤搜尋時優先使用Obsidiantag命令，而不是進行全文搜尋。好，接下來我們來驗證一下。我們在Obsidian中就不用手動輸入命令了。就可以直接跟Obsidian說幫我建立一篇筆記。標題叫AI建立的筆記。筆記的內容是今天的日期。然後CloudCode就調動了ObsidianCLI的這個skill。

### [00:03:59]
並且使用 create 命令 建立了 筆記。名字叫 AI 建立 筆記。內容是 今天的日期。仔細 看 這個 簡單 的 操作。其實 也是 疊加 了 多個 命令 在一起。好，我們到Obsidian中看看剛才的這個筆記已經建立成功了。內容也沒有問題。那麼恭喜你又教會了AI一個新的技能。剛才演示了基礎的用法。接下來我們聊一個更深一點的問題。看到這裡聰明的你會說了：現在的Cloudian外掛不是也能讓CloudCode來閱讀和修改Obsidian的筆記嗎？為什麼還要使用ObsidianCLI呢？接下來我們就來聊聊這兩者之間的差異。

### [00:04:38]
首先我們要對Obsidian建立起兩層的認知。第一層，Obsidian說簡單一點。就是一個Markdown編輯器。對Markdown的檔案進行增刪改查。跟眾多的Markdown編輯器差不多。甚至在顏值體驗方面還不是最好的。第二層Obsidian又不僅是Markdown編輯器。為了做更復雜的知識管理，Obsidian的雙項鍊：標籤。屬性基礎這些能力都是超越了Markdown標準的存在跟普通的Markdown編輯器就不一樣了對於第一層Markdown檔案編輯所有的AIAgent天生都具備這個能力因為它本質上就是對純文字檔案的讀取和編輯簡單說就是讀文字但到了第二層要理解雙項鍊

### [00:05:21]
筆記屬性：標籤：這些複雜的結構：AIAgent天生的能力就不太夠了：他們只能一個個地去讀取檔案：去推斷費時費力還容易出錯更別提Token的消耗了而雙子座這些資訊Obsidian的內部是直接知道的現在Obsidian通過了CLI。就等於是直接告訴AI。你不用一個個去推斷了。我直接告訴你了。說人話就是懂關係。

### [00:05:46]
你看這兩層關係的第一層：像不像是做牛馬時候的你？每天的主要工作就是看文件，寫文件。而第二層像不像是當了大領導之後的你？每天的主要工作就是做連線疏通關係職場的哲學和產品的哲學竟然如此相通我們還是用查反向連結來舉一個例子比如你想知道有哪些筆記提到了《復利思維》這篇筆記《復利思維》這篇筆記的markdown檔案裡並沒有提到其他哪篇筆記提到我而查詢的方法，你得一篇一篇地去找。

### [00:06:19]
比如開啟健身筆記，找到它連結到了負面思維，你要依次開啟每條筆記。透過文字匹配去看它有沒有提到負面思維最終才能得到答案這種方式效率低而且容易遺漏其實這個關係在Observing中本身就已經存在如果透過CLI的方式查詢我們只需要一行命令瞬間查出相關的內容因為Obseding已經建立起了完整的連結所以好接下來我們來看一個比較有趣的場景比如我想分析在我的筆記庫中

### [00:06:48]
有哪些筆記同時提到了AI和知識管理兩個標籤但是，AI可能包括多種AI開頭的標籤。比如AI工具，AI概念，AI學習等等。我只要用比較簡單的語言告訴CloudCode用Obsidian專門搜尋標籤的命令來同時找AI開頭的標籤和知識管理標籤的筆記這個時候它就開始啟用Obsidian CLI當然他會先學習一下先用了help去了解然後他就開始幹活了他進入了我的筆記庫然後去學習這個標簽是什麼回事

### [00:07:25]
對然後就開始找標簽了AI標簽AI概念標籤AI時代怎樣學習知識管理透過這些標籤找到筆記然後最後進行一個彙總那找到這些筆記之後，第二步，我就是讓他在Obsidian中建立一個Kammos檔案，中心節點呢，就是叫AI知識管理這個主題然後第二級的節點呢就是這些具體的AI標簽就是它前面找到的這些AI工具啊AI概念啊作為二級的節點，那三級的節點呢？

### [00:07:55]
就是具體的筆記文章。當然這也是用到了JSONCanvas這個skill。去在Obsidian中建立Canvas檔案。然後這裡就已經建立完成了這個Canvas檔案了。讓我們在Obsidian中看看吧。這裡就彙集了我之前所有的關於AI加知識管理相關思考的筆記。在這裡思維變成了一棵樹。以這樣的方式回顧之前的思考體驗真的是很不一樣。如果在這個過程中你產生了新的洞見，也可以直接記錄或者新增新的筆記。好，我們再來總結一下。對於單條筆記一類的文稿進行編輯。例如潤色修改等等適合用agent直接查改對於需要跨多條筆記進行關係梳理和洞察，就需要用到我們的ObsidianCLI以前AI只能一篇一篇翻你的筆記，看到的都是碎片。

### [00:08:48]
現在有了CLI。AI能看懂你的筆記是怎麼連線起來的。有的朋友可能會說。我的筆記還很少。知識網路沒有建立起來那我也用不起來恰恰相反現在就是最好的起點你的每一回記錄AI都能理解它在整個知識體系中的位置。這就是從0到1的複利起點。之後我還會分享。如何用Obsidian加CLI。構建起你的第一個知識網路。並讓AI第一天就能幫到你。

### [00:09:13]
關注我。讓AI成為你的知識助手。

